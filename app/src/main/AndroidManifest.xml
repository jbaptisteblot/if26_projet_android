<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.utt.if26.if26_sncfprojet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivityTemp"/>
        <activity
            android:name=".SearchPlace"
            android:label="@string/title_activity_search_place"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".AddTrajet"
            android:label="@string/title_activity_addTrajet" />
        <activity
            android:name=".ShowNextDeparture"
            android:label="@string/title_activity_departure" />
        <activity android:name=".ShowDepartsGare"
            android:label="@string/title_activity_departure" />
    </application>

</manifest>